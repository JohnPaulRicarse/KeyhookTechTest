%article.article
  %section.section
    .section-header
      You editing the Booking for:
      = @availability.scheduled_date.to_fs(:full_readable)

  %section.section
    = render "form", booking: @booking, path: availability_booking_path(@availability, @booking)
